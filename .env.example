# Copy this file to .env and customize the values for your environment.
MSSQL_SERVER=your-sql-hostname
MSSQL_DB=master
MSSQL_USER=app_user
MSSQL_PASSWORD=super-secret-password
# Set MSSQL_AUTH to windows to use integrated auth (user/password are ignored in that case)
MSSQL_AUTH=sql
# Windows + pyodbc: override the driver name if needed (e.g., "ODBC Driver 17 for SQL Server")
MSSQL_DRIVER=ODBC Driver 18 for SQL Server
# Optional: set MSSQL_PORT to force a port; leave blank to use the default or named instance resolution
MSSQL_PORT=
# Optional toggles: set to 1/true/on
# MSSQL_USE_PYODBC=1  # Force pyodbc on non-Windows
# MSSQL_USE_PYMSSQL=1 # Force pymssql on Windows
